class Collision {

    constructor(scene, object){

    }

    function inside(action){

        var object_pos_x;
        var object_pos_y;
        var object_width;
        var object_height;

    }

    function outside(action){

        var object_pos_x;
        var object_pos_y;
        var object_width;
        var object_height;
    	
    }

    function rang(action){

        var object_pos_x;
        var object_pos_y;
        var object_width;
        var object_height;
    	
    }

}